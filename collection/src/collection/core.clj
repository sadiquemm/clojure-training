(ns collection.core
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]

;;list
(def list1 '(1 2 3))
(println list1)

;;set
(def set1 #{1 3 5})
(println set1)

;;vector
(def vect [1 0 3])
(println vect)
  
  )
